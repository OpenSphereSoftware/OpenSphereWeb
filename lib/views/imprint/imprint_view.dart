

import 'package:flutter/material.dart';
import 'package:opensphereweb/views/layout_template/layout_template.dart';

class ImprintView extends StatelessWidget {
  const ImprintView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return LayoutTemplate(child: Container(color: Colors.red,));
  }
}